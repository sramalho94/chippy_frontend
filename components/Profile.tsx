// import React from 'react';
// import axios from 'axios'

// type User = {
//   username: string;
//   firstName: string;
//   lastName: string;
//   passwordDigest: string;
// };

// type ProfileProps = {
//   user: User;
// };

// const Profile: React.FC<ProfileProps> = ({ user }) => (
//   <div className="max-w-sm mx-auto bg-white shadow-lg rounded-lg overflow-hidden my-10">
//     <div className="sm:flex sm:items-center px-6 py-4">
//       <div className="text-center sm:text-left flex-shrink-0 h-16 sm:h-24 rounded-full bg-gray-300 flex items-center justify-center text-2xl sm:text-4xl text-white">
//         {user.firstName[0].toUpperCase()}
//       </div>
//       <div className="mt-4 sm:mt-0 sm:ml-4 text-center sm:text-left">
//         <p className="text-xl leading-tight">{`${user.firstName} ${user.lastName}`}</p>
//         <p className="text-sm leading-tight text-gray-600">@{user.username}</p>
//       </div>
//     </div>
//   </div>
// );

// export default Profile;

